name: Automatic comment on the latest PR

on: 
  pull_request_target:
    types: [opened, edited, synchronize, reopened]
jobs:
  run:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.7'    
      
      - name: Comment on the latest PR
        run: |
          pip install requests
          python create_pr_comment.py --token ${{ secrets.GITHUB_TOKEN }}

